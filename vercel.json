{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "prisma generate && prisma migrate deploy && POSTGRES_PRISMA_URL=$POSTGRES_URL_NON_POOLING prisma db seed && next build"
}
